---
import LocalePlaceholderPage from '@/components/LocalePlaceholderPage.astro';
import {
  getLocaleNestedPaths,
  type LocalePlaceholderPageProps,
} from '@/data/localePlaceholders';

export function getStaticPaths() {
  return getLocaleNestedPaths();
}

const props = Astro.props as LocalePlaceholderPageProps;
---

<LocalePlaceholderPage {...props} />
